spring:
  application:
    name: scb-campaign-service
  cloud:
    bus:
      enabled: true

campaign:
  enabled: false
  type: PREMIUM-TEST
  discount-rate: 40
  response:
    message-prefix: "[CAMPAIGN]"
    include-discount: false

management:
  endpoints:
    web:
      exposure:
        include: health,info,env
